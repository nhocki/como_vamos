<h1 class='title'><%= t('.title') %></h1>

<div class='subtitle'>
  <% if logged_in? %>
    <%= link_to t('.add_judge'), new_judge_path %>
  <% else %>
    <%= raw t('.guest_add_judge', login: link_to(t('.login_text'), login_path(:github))) %>
  <% end %>
</div>

<div class='judges'><%= render @judges %></div>
