<h1 class='title'><%= t('.title') %></h1>

<div class='subtitle'>
  <%= t('.subtitle', problem: link_to(@problem.name, @problem), judge: link_to(@problem.judge_name, @problem.judge)).html_safe %>
</div>

<%= render 'form', solution: @solution %>
